export type NewUser = {
  email: string;
  name: string;
  password: string;
  projectId: string;
  projectName: string;
};

export type User = {
  id: string;
  email: string;
  name: string;
  projectId: string;
  projectName: string;
};
