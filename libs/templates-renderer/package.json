{
  "name": "@muil/templates-renderer",
  "version": "1.3.3",
  "license": "MIT",
  "main": "src/index.js",
  "files": [
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "prepublish": "rimraf dist && mv src temp_src && npm run build && cp -r ../../dist/libs/templates-renderer/src src",
    "postpublish": "rimraf dist && mv temp_src src"
  },
  "dependencies": {
    "@nestjs/common": "^7.5.3",
    "html-minifier": "^4.0.0",
    "juice": "^7.0.0",
    "puppeteer-core": "^5.3.1",
    "reflect-metadata": "^0.1.13",
    "request": "^2.88.2",
    "rxjs": "^6.5.4",
    "uuid": "^8.3.1",
    "vm2": "^3.9.2"
  },
  "peerDependencies": {
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "styled-components": "^5.2.0"
  },
  "devDependencies": {
    "@types/html-minifier": "^4.0.0",
    "@types/puppeteer-core": "^2.0.0",
    "@types/require-from-string": "^1.2.0"
  }
}
